// Making my life easier
const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;

export const MAX_FILE_SIZE = 100 * 1024; // 100KB
export const MAX_FILE_LIFETIME = 3 * DAY; // Hey, the space is limited!
